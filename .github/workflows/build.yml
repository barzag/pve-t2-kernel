name: Build T2 kernel

on:
  workflow_dispatch:
  schedule:
    - cron: '17 */6 * * *'

concurrency:
  group: t2-kernel-build-and-release
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  discover:
    name: Discover and build changed upstream revisions
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    outputs:
      changed: ${{ steps.candidate.outputs.changed }}
      tag: ${{ steps.candidate.outputs.tag }}
      kernel: ${{ steps.pve_repo.outputs.kernel }}
      pve_sha: ${{ steps.pve.outputs.sha }}
      t2_sha: ${{ steps.t2.outputs.sha }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Discover latest published Proxmox kernel
        id: pve_repo
        shell: bash
        run: |
          bash scripts/discover-proxmox.sh

      - name: Resolve published Proxmox source commit
        id: pve
        shell: bash
        env:
          PUBLISHED_KERNEL: ${{ steps.pve_repo.outputs.kernel }}
          PUBLISHED_VERSION: ${{ steps.pve_repo.outputs.version }}
        run: |
          bash scripts/resolve-proxmox.sh

      - name: Resolve T2 patch candidate
        id: t2
        shell: bash
        env:
          PVE_KERNEL: ${{ steps.pve_repo.outputs.kernel }}
        run: |
          bash scripts/resolve-t2.sh

      - name: Check whether these sources are already published
        id: candidate
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          PVE_SHA: ${{ steps.pve.outputs.sha }}
          T2_SHA: ${{ steps.t2.outputs.sha }}
          KERNEL_VERSION: ${{ steps.pve_repo.outputs.kernel }}
          PACKAGE_VERSION: ${{ steps.pve_repo.outputs.version }}
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import hashlib, json, os, subprocess, urllib.error, urllib.request
          e = os.environ
          # Include the build recipe, but ignore unrelated documentation commits.
          recipe = subprocess.check_output([
              'git', 'rev-parse', 'HEAD:scripts', 'HEAD:.github/workflows/build.yml'
          ]).decode()
          identity = '\n'.join([e['PVE_SHA'], e['T2_SHA'], e['PACKAGE_VERSION'], recipe])
          digest = hashlib.sha256(identity.encode()).hexdigest()
          tag = f"auto-{e['KERNEL_VERSION']}-pve-t2-{digest}"
          request = urllib.request.Request(
              f"https://api.github.com/repos/{e['GITHUB_REPOSITORY']}/releases/tags/{tag}",
              headers={'Authorization': 'Bearer ' + e['GH_TOKEN'],
                       'Accept': 'application/vnd.github+json'})
          try:
              with urllib.request.urlopen(request, timeout=60) as response:
                  release = json.load(response)
              changed = release['draft']
          except urllib.error.HTTPError as error:
              if error.code != 404:
                  raise
              changed = True
          with open(e['GITHUB_OUTPUT'], 'a') as output:
              output.write(f"tag={tag}\nchanged={str(changed).lower()}\n")
          with open(e['GITHUB_STEP_SUMMARY'], 'a') as summary:
              summary.write(f"Candidate: `{tag}`\n\n")
              summary.write('New or unpublished sources: build required.\n' if changed
                            else 'Already published: compilation and publication skipped.\n')
          PY

      - name: Test T2 patch application
        if: steps.candidate.outputs.changed == 'true'
        id: compat
        shell: bash
        env:
          PVE_SHA: ${{ steps.pve.outputs.sha }}
          T2_SHA: ${{ steps.t2.outputs.sha }}
        run: |
          bash scripts/validate-patches.sh

      - name: Prepare T2 build tree for Macmini8,1
        if: steps.candidate.outputs.changed == 'true'
        id: integrate
        shell: bash
        env:
          PVE_SHA: ${{ steps.pve.outputs.sha }}
          T2_SHA: ${{ steps.t2.outputs.sha }}
          EXPECTED_PATCH_COUNT: ${{ steps.compat.outputs.patch_count }}
        run: |
          bash scripts/prepare-integration.sh

      - name: Build Macmini8,1 T2 kernel
        if: steps.candidate.outputs.changed == 'true'
        id: build
        shell: bash
        run: |
          set -euo pipefail

          PVE_DIR="proxmox-pve-kernel"
          KERNEL_VERSION="${{ steps.pve_repo.outputs.kernel }}"

          echo "Starting T2 kernel build"
          echo "Target:  Macmini8,1"
          echo "Kernel:  ${KERNEL_VERSION}-pve-t2"

          #
          # Free disk space on the ephemeral GitHub runner.
          #

          echo "Disk space before cleanup:"
          df -h /

          sudo rm -rf \
            /usr/local/lib/android \
            /usr/share/dotnet \
            /opt/ghc \
            /usr/local/.ghcup \
            2>/dev/null || true

          docker image prune -af >/dev/null 2>&1 || true

          echo "Disk space after cleanup:"
          df -h /

          #
          # Pin both Proxmox submodules to their official sources.
          #

          git -C "${PVE_DIR}" config \
            submodule.submodules/ubuntu-kernel.url \
            https://git.proxmox.com/git/mirror_ubuntu-kernels.git

          git -C "${PVE_DIR}" config \
            submodule.submodules/zfsonlinux.url \
            https://git.proxmox.com/git/zfsonlinux.git

          #
          # Build in Debian Trixie.
          #

          DOCKER_IMAGE="debian:trixie@sha256:f324c7ff54321e8d9c588493a20244965938ce0aa50bbd1022d38010e9ffc4b1"

          docker run \
            --rm \
            -v "${PWD}:/workspace" \
            -w "/workspace/${PVE_DIR}" \
            "${DOCKER_IMAGE}" \
            bash -lc '
              set -euo pipefail
              export DEBIAN_FRONTEND=noninteractive

              echo "Installing Debian prerequisites..."

              apt-get update -qq

              apt-get install -y -qq \
                --no-install-recommends \
                ca-certificates \
                curl \
                >/dev/null

              curl \
                --fail \
                --location \
                --silent \
                --show-error \
                https://enterprise.proxmox.com/debian/proxmox-archive-keyring-trixie.gpg \
                -o /usr/share/keyrings/proxmox-archive-keyring.gpg

              cat > /etc/apt/sources.list.d/proxmox.sources <<EOF
          Types: deb
          URIs: http://download.proxmox.com/debian/pve
          Suites: trixie
          Components: pve-no-subscription
          Signed-By: /usr/share/keyrings/proxmox-archive-keyring.gpg
          EOF

              apt-get update -qq

              echo "Installing Proxmox kernel build dependencies..."

              apt-get install -y -qq \
                --no-install-recommends \
                asciidoc-base \
                automake \
                bc \
                bindgen \
                bison \
                build-essential \
                cpio \
                debhelper \
                dh-python \
                dpkg-dev \
                dwarves \
                fakeroot \
                file \
                flex \
                gawk \
                gcc \
                git \
                kmod \
                libdw-dev \
                libelf-dev \
                libiberty-dev \
                libnuma-dev \
                libpve-common-perl \
                libslang2-dev \
                libssl-dev \
                libtool \
                lintian \
                lz4 \
                perl-modules \
                python3-dev \
                python3-minimal \
                rsync \
                rust-clippy \
                rust-src \
                rustc \
                rustfmt \
                sphinx-common \
                xmlto \
                zlib1g-dev \
                zstd \
                >/dev/null

              git config \
                --global \
                --add safe.directory \
                /workspace/proxmox-pve-kernel

              KERNEL_MAJ="$(
                sed -n "s/^KERNEL_MAJ=//p" Makefile |
                xargs
              )"

              KERNEL_MIN="$(
                sed -n "s/^KERNEL_MIN=//p" Makefile |
                xargs
              )"

              KERNEL_PATCHLEVEL="$(
                sed -n "s/^KERNEL_PATCHLEVEL=//p" Makefile |
                xargs
              )"

              if [[ ! "${KERNEL_MAJ}" =~ ^[0-9]+$ ]] ||
                 [[ ! "${KERNEL_MIN}" =~ ^[0-9]+$ ]] ||
                 [[ ! "${KERNEL_PATCHLEVEL}" =~ ^[0-9]+$ ]]
              then
                echo "ERROR: unable to determine Proxmox kernel base version"
                exit 1
              fi

              KERNEL_BASE="${KERNEL_MAJ}.${KERNEL_MIN}.${KERNEL_PATCHLEVEL}"

              BUILD_KERNEL_DIR="/workspace/proxmox-pve-kernel/proxmox-kernel-${KERNEL_BASE}/ubuntu-kernel"

              echo "Kernel base: ${KERNEL_BASE}"
              echo "Allowing Proxmox build kernel directory:"
              echo "${BUILD_KERNEL_DIR}"

              git config \
                --global \
                --add safe.directory \
                "${BUILD_KERNEL_DIR}"

              echo "Initializing exact Proxmox submodules..."

              git submodule update \
                --init \
                --depth 1 \
                submodules/ubuntu-kernel

              git submodule update \
                --init \
                --recursive \
                submodules/zfsonlinux

              echo
              echo "Building kernel..."
              echo

              make deb
            '

      - name: Validate generated T2 packages
        if: steps.candidate.outputs.changed == 'true'
        id: packages
        shell: bash
        env:
          PVE_DIR: proxmox-pve-kernel
          KERNEL_VERSION: ${{ steps.pve_repo.outputs.kernel }}
        run: |
          bash scripts/validate-packages.sh

      - name: Upload T2 kernel artifacts
        if: steps.candidate.outputs.changed == 'true'
        id: upload
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: pve-t2-${{ steps.pve_repo.outputs.kernel }}-macmini8-1
          path: |
            proxmox-pve-kernel/proxmox-kernel-*-pve-t2_*.deb
            proxmox-pve-kernel/proxmox-headers-*-pve-t2_*.deb
            proxmox-pve-kernel/SHA256SUMS
          if-no-files-found: error
          compression-level: 0
          retention-days: 7

      - name: Write build summary
        if: always()
        shell: bash
        run: |
          cat >> "$GITHUB_STEP_SUMMARY" <<'EOF'
          # T2 kernel workflow

          ## Target
          - Hardware: `Macmini8,1`

          ## Published Proxmox kernel
          - Repository: `pve-no-subscription`
          - Package: `${{ steps.pve_repo.outputs.package }}`
          - Kernel: `${{ steps.pve_repo.outputs.kernel }}`
          - Package version: `${{ steps.pve_repo.outputs.version }}`

          ## Proxmox source
          - SHA: `${{ steps.pve.outputs.sha }}`
          - Commit: `${{ steps.pve.outputs.subject }}`
          - Debian changelog: `${{ steps.pve.outputs.changelog }}`

          ## T2 source
          - SHA: `${{ steps.t2.outputs.sha }}`
          - Kernel target: `${{ steps.t2.outputs.kver }}`
          - Commit: `${{ steps.t2.outputs.subject }}`

          ## Compatibility
          - Baseline compatible: `${{ steps.compat.outputs.compatible }}`
          - T2 patches applied: `${{ steps.compat.outputs.patch_count }}`
          - T2 patches skipped: `${{ steps.compat.outputs.skipped_count }}`
          - Skipped patch: `${{ steps.compat.outputs.skipped_patch }}`
          - Failed patch: `${{ steps.compat.outputs.failed_patch }}`

          ## Integration
          - Target: `${{ steps.integrate.outputs.target }}`
          - T2 patches staged: `${{ steps.integrate.outputs.patch_count }}`
          - T2 config options: `${{ steps.integrate.outputs.config_count }}`
          - Kernel suffix: `${{ steps.integrate.outputs.suffix }}`

          ## Result
          - Kernel build: `${{ steps.build.outcome }}`
          - Package validation: `${{ steps.packages.outcome }}`
          - Artifact upload: `${{ steps.upload.outcome }}`
          EOF

  publish:
    name: Publish validated kernel prerelease
    needs: discover
    if: needs.discover.outputs.changed == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: write
      actions: read
    env:
      GH_TOKEN: ${{ github.token }}
      GH_REPO: ${{ github.repository }}
      RELEASE_TAG: ${{ needs.discover.outputs.tag }}
      KERNEL_VERSION: ${{ needs.discover.outputs.kernel }}
      PVE_SHA: ${{ needs.discover.outputs.pve_sha }}
      T2_SHA: ${{ needs.discover.outputs.t2_sha }}
    steps:
      - name: Download validated packages from this run
        shell: bash
        run: |
          set -euo pipefail
          gh run download "$GITHUB_RUN_ID" \
            --name "pve-t2-${KERNEL_VERSION}-macmini8-1" \
            --dir release-assets

      - name: Verify packages and prepare portable checksums
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import hashlib, os, pathlib, re, subprocess
          root = pathlib.Path('release-assets')
          files = list(root.iterdir())
          assert len(files) == 3 and all(p.is_file() for p in files), 'Expected exactly three files'
          kernel = list(root.glob('proxmox-kernel-*-pve-t2_*.deb'))
          headers = list(root.glob('proxmox-headers-*-pve-t2_*.deb'))
          assert len(kernel) == len(headers) == 1, 'Expected kernel and headers'
          packages = kernel + headers
          sums = root / 'SHA256SUMS'
          expected = {}
          for line in sums.read_text().splitlines():
              digest, name = line.split(maxsplit=1)
              name = pathlib.PurePosixPath(name).name
              assert re.fullmatch(r'[0-9a-f]{64}', digest) and name not in expected
              expected[name] = digest
          assert set(expected) == {p.name for p in packages}, 'Invalid checksum coverage'
          versions = []
          for p, prefix in zip(packages, ['proxmox-kernel-', 'proxmox-headers-']):
              assert hashlib.sha256(p.read_bytes()).hexdigest() == expected[p.name], 'Checksum mismatch'
              name, version, arch = subprocess.check_output([
                  'dpkg-deb', '-W', '--showformat=${Package}\t${Version}\t${Architecture}', str(p)
              ], text=True).split('\t')
              assert name == prefix + os.environ['KERNEL_VERSION'] + '-pve-t2'
              assert arch == 'amd64'
              versions.append(version)
          assert versions[0] == versions[1], 'Kernel/header version mismatch'
          # Users can run sha256sum -c SHA256SUMS beside the downloaded packages.
          sums.write_text(''.join(f'{expected[p.name]}  {p.name}\n' for p in packages))
          e = os.environ
          pathlib.Path('release-notes.md').write_text(
              f"Proxmox kernel {e['KERNEL_VERSION']}-pve-t2 for Macmini8,1\n\n"
              'Automatically compiled and package-validated. Hardware boot, thermal sensors '
              'and fan operation have NOT been validated by this workflow.\n\n'
              'Profile: AppleSMC / sensors / fan only; not the complete T2 patchset.\n\n'
              f"Proxmox source: {e['PVE_SHA']}\n\nT2 source: {e['T2_SHA']}\n\n"
              f"Build recipe commit: {e['GITHUB_SHA']}\n\n"
              f"Build: https://github.com/{e['GITHUB_REPOSITORY']}/actions/runs/{e['GITHUB_RUN_ID']}\n\n"
              'Assets: kernel, matching headers and SHA256SUMS. '
              'Verify with `sha256sum -c SHA256SUMS`. Keep a working kernel for rollback.\n'
          )
          PY

      - name: Stage assets then publish prerelease
        shell: bash
        run: |
          set -euo pipefail
          # Only a genuine 404 means missing; API/authentication errors must fail.
          STATUS="$(curl --silent --show-error --output release.json --write-out '%{http_code}' \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H 'Accept: application/vnd.github+json' \
            "https://api.github.com/repos/${GH_REPO}/releases/tags/${RELEASE_TAG}")"
          case "$STATUS" in
            200)
              if [[ "$(jq -r '.draft' release.json)" != true ]]; then
                echo 'This release is already published; keeping existing assets.'
                exit 0
              fi
              ;;
            404)
              gh release create "$RELEASE_TAG" --draft --prerelease --latest=false \
                --target "$GITHUB_SHA" \
                --title "Proxmox ${KERNEL_VERSION} T2 — Macmini8,1" \
                --notes-file release-notes.md
              ;;
            *) echo "ERROR: release lookup returned HTTP ${STATUS}"; exit 1 ;;
          esac
          # Resume an incomplete draft if a previous upload failed.
          gh release upload "$RELEASE_TAG" release-assets/* --clobber
          gh release edit "$RELEASE_TAG" --draft=false --prerelease --latest=false \
            --notes-file release-notes.md
          printf 'Published prerelease: https://github.com/%s/releases/tag/%s\n' \
            "$GH_REPO" "$RELEASE_TAG" >> "$GITHUB_STEP_SUMMARY"
